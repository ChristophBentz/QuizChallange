# Umgebungsvariablen für Produktionsumgebung
# Kopieren Sie diese Datei zu .env und tragen Sie Ihre echten Werte ein

# Flask-Konfiguration
FLASK_ENV=production
SECRET_KEY=ihr-super-sicherer-secret-key-hier

# Datenbank (Optional - standardmäßig wird SQLite verwendet)
# DATABASE_URL=sqlite:///instance/users.db

# E-Mail-Konfiguration
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=ihre-email@gmail.com
MAIL_PASSWORD=ihr-app-passwort
MAIL_DEFAULT_SENDER=ihre-email@gmail.com

# Für MySQL/MariaDB (falls gewünscht):
# DATABASE_URL=mysql://username:password@localhost/datenbankname

# Für PostgreSQL (falls gewünscht):
# DATABASE_URL=postgresql://username:password@localhost/datenbankname 